MRANTI CRM V9 CORRECTION

1. Upload every file in this folder to the repository root.
2. Replace the V8 favicon and icon files with these V9 files.
3. Keep the existing n8n workflow; no workflow change is required.
4. Wait for GitHub Pages deployment to finish.
5. Close all CRM tabs, reopen the page, and use Ctrl+Shift+R.

This correction uses the exact user-provided network mark for all icons and fixes the
desktop overflow that clipped the word intelligence. The logged-in CRM code is unchanged.
